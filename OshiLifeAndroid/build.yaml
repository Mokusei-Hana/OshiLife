targets:
  $default:
    builders:
      drift_dev:
        options:
          # Dates are persisted as ISO-8601 UTC text — aligned with the
          # ISO-8601 dates iOS already writes in pending-import payloads,
          # and human-readable for a future export format.
          store_date_time_values_as_text: true
